@model SenseLib.Models.SystemConfig

@{
    ViewData["Title"] = "Chi tiết cấu hình";
    Layout = "_AdminLayout";
    bool isPointConfig = Model.ConfigKey.Contains("Point") || Model.Description.Contains("Point") || Model.Description.Contains("point");
}

<section class="section dashboard">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">
                        @if (isPointConfig)
                        {
                            <i class="bi bi-coin me-2 text-primary"></i>
                            <span class="text-primary">Chi tiết cấu hình Point</span>
                        }
                        else
                        {
                            <i class="bi bi-gear me-2"></i>
                            <span>Chi tiết cấu hình hệ thống</span>
                        }
                    </h5>
                    
                    @if (isPointConfig && Model.ConfigKey == "PointsForApprovedDocument")
                    {
                        <div class="alert alert-info mb-4">
                            <div class="d-flex">
                                <div class="me-3">
                                    <i class="bi bi-lightbulb-fill fs-3"></i>
                                </div>
                                <div>
                                    <strong>Thông tin quan trọng</strong><br>
                                    <p class="mb-1">Cấu hình này xác định số Point người dùng nhận được khi tài liệu họ đăng tải lên hệ thống được admin phê duyệt.</p>
                                    <p class="mb-0">Mỗi khi một tài liệu được phê duyệt, hệ thống sẽ tự động cộng số Point này vào ví của người đăng tài liệu.</p>
                                </div>
                            </div>
                        </div>
                    }
                    
                    <div class="row mb-4">
                        <div class="col-lg-12">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered">
                                    <tbody>
                                        <tr>
                                            <th scope="row" style="width: 25%">ID</th>
                                            <td>@Model.ConfigID</td>
                                        </tr>
                                        <tr>
                                            <th scope="row">Khóa cấu hình</th>
                                            <td><strong>@Model.ConfigKey</strong></td>
                                        </tr>
                                        <tr>
                                            <th scope="row">
                                                @if (isPointConfig) { <span>Số Point</span> } else { <span>Giá trị</span> }
                                            </th>
                                            <td>
                                                @if (isPointConfig)
                                                {
                                                    <span class="badge bg-primary fs-6">@Model.ConfigValue</span>
                                                    <span> Point</span>
                                                }
                                                else
                                                {
                                                    @Model.ConfigValue
                                                }
                                            </td>
                                        </tr>
                                        <tr>
                                            <th scope="row">Mô tả</th>
                                            <td>@Html.Raw(Model.Description?.Replace(Environment.NewLine, "<br />"))</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    
                    @if (Model.ConfigKey == "PointsForApprovedDocument")
                    {
                        <div class="card mb-4">
                            <div class="card-header bg-light">
                                <h6 class="mb-0"><i class="bi bi-info-circle me-1"></i>Quy trình cộng Point</h6>
                            </div>
                            <div class="card-body">
                                <ol>
                                    <li>Người dùng đăng tải tài liệu lên hệ thống</li>
                                    <li>Admin xem xét và phê duyệt tài liệu trong phần quản trị</li>
                                    <li>Hệ thống tự động cộng <strong>@Model.ConfigValue Point</strong> vào ví của người dùng</li>
                                    <li>Giao dịch được ghi lại trong lịch sử ví của người dùng</li>
                                    <li>Người dùng có thể sử dụng Point để mua các tài liệu trả phí khác</li>
                                </ol>
                            </div>
                        </div>
                    }
                    
                    <div class="text-center">
                        <a asp-action="Edit" asp-route-id="@Model.ConfigID" class="btn btn-primary">
                            <i class="bi bi-pencil"></i> Chỉnh sửa
                        </a>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Quay lại
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section> 